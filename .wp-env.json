{
  "plugins": [ "." ],
  "lifecycleScripts": {
    "afterStart": "docker exec -u 0 $(docker ps -q --filter 'name=wordpress' --filter 'status=running' | head -1) bash -c 'apt-get update -qq && apt-get install -y -qq imagemagick graphicsmagick'"
  },
  "testsEnvironment": false
}
